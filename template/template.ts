import { getInput, getInputLines } from "../shared/shared";

(async () => {
  const fullInput = await getInput(__dirname);
  const inputLines = await getInputLines(__dirname);

  // Rest of code

})();